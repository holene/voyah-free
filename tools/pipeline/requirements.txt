# PDF extraction pipeline (Python 3.11+)
pymupdf>=1.24.0
jsonschema>=4.22.0

# Optional: OCR for scanned PDFs (requires Tesseract on PATH)
# pytesseract>=0.3.10
# pillow>=10.0.0

# Optional: LLM stages (canonicalize / translate)
# openai>=1.40.0
